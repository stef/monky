system monitor for ANSI and TMUX terminals.

ANSI mode run with:
./mon sda eth1:

TMUX mode:
./mon sda eth1: /dev/shm/tmux-mon

in tmux do:
set-option -g status-right "#(tail -1 /dev/shm/tmux-mon)"
and enjoy your mon-in-tmux.

no configuration, stuff gets compiled, check the defines.
